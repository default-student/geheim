interface:
  display_name: "Geheim Credentials"
  short_description: "Run commands with credentials without disclosure"
  default_prompt: "Use $geheim-credentials to run this command with the required credential safely."
